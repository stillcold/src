all : ${PROGS}

${PROGS} : ${COMPILEOBJ}
	${CC}  -o $(BINDIR)/$(PROGS) $(BINDIR)/$(COMPILEOBJ)

${COMPILEOBJ} : ${SOURCECODE}
	${CC} -c ${CFLAGS} $(CWARNINGFLAGS) -o $(BINDIR)/$(COMPILEOBJ) $(SOURCECODE)

